#pragma once

// not finished yet
// i want to make custom scriptinh language

#include "pch.h"
#include "Lexer.h"

namespace FRL {
	class Interpreter {
		public:
			void Initialize(std::string code);

		private:

	};
};